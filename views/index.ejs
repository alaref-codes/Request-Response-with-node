<!DOCTYPE html>
<html lang="en">

    <%- include('./partials/head.ejs')   %>

<body>

    <%- include('./partials/nav.ejs')   %>
    
    <div class="blog content">
        <% if (blogs.length) { %>
            <% blogs.forEach(blog => { %>
            <h1> <%= blog.title %> </h1>
            <% }) %>
        <% } else { %>
            <p> There is no blogs for the moment </p>
        <% } %>
    </div>

    <%- include('./partials/footer.ejs')   %>

</body>
</html>